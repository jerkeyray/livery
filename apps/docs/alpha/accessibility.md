# Accessibility

Rendered SVG has an accessible figure label and description. Standard components expose documented label parameters and deterministic reading order. Timeline controls use native buttons, announce current state, and work with arrow, Home, and End keys.

Meaningful state always has a non-color cue. Callers should provide an accessible text fallback for figures whose full meaning is not represented by labels alone.
