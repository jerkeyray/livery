# Migration

Legacy `flow` source remains accepted through a compatibility translator and emits a deprecation diagnostic. No legacy public API is removed in this alpha cycle.

Move toward `figure`, standard-library calls, anchor connectors, and named timeline states. Treat all pre-1.0 APIs as unstable unless explicitly documented as frozen; the single-file visual syntax is frozen for the public alpha.
