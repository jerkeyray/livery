# Timelines

Timelines modify stable identities without recomputing board geometry. States support `show`, `hide`, `focus`, `trace`, and `set`; unsupported morph pairs fail explicitly.

<<< ../../../fixtures/visual/timeline.livery{livery}

Transitions honor theme duration tokens and the user’s reduced-motion preference. Every state keeps connector endpoints, labels, and component descendants consistent.
