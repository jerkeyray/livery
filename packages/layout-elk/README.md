# @livery/layout-elk

Optional ELK adapters for cycles and complex Livery graphs. Use the worker adapter in browsers and the in-process adapter in headless environments.

```ts
import { createElkLayoutAdapter } from "@livery/layout-elk";

const adapter = createElkLayoutAdapter();
```
